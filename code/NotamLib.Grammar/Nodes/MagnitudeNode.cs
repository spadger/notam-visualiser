using Irony.Ast;
using Irony.Interpreter.Ast;
using Irony.Parsing;

namespace NotamLib.Parsing.Nodes
{
    public class MagnitudeNode : AstNode
    {
        public override void Init(AstContext context, ParseTreeNode treeNode)
        {
            base.Init(context, treeNode);
        }
    }
}